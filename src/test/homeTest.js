import { render } from '@testing-library/react';
import Home from './Home';

test('renders Home component', () => {
  render(<Home />);
  // Add assertions here to verify that the component is rendered correctly
});

test('renders Navigation Bar', () => {
  render(<Home />);
  // Add assertions here to verify that the Navigation Bar is rendered correctly
});

test('renders Background image', () => {
  render(<Home />);
  // Add assertions here to verify that the Background image is rendered correctly
});

test('renders Welcome header', () => {
  render(<Home />);
  // Add assertions here to verify that the Welcome header is rendered correctly
});

test('renders Summary text', () => {
  render(<Home />);
  // Add assertions here to verify that the Summary text is rendered correctly
});

test('renders Projects button', () => {
  render(<Home />);
  // Add assertions here to verify that the Projects button is rendered correctly
});

test('renders GitHub button', () => {
  render(<Home />);
  // Add assertions here to verify that the GitHub button is rendered correctly
});

test('renders LinkedIn button', () => {
  render(<Home />);
  // Add assertions here to verify that the LinkedIn button is rendered correctly
});